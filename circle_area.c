#include <stdio.h>
#include <stdlib.h>

int main(void) {
    int side = 100;
    int trials = 1000;

    double x, y;
    int radius = side/2;

    for (int i=0; i<trials; ++i) {
        x = rand()%
    }
    return 0;
}
